const getUserById = async (id) => {
  const user = {};

  return user;
};

const getUserByEmail = async (email) => {
  const user = {};

  return user;
};

const logoutUser = async (token) => {
  const user = {};

  return user;
};

module.exports = {
  getUserById,
  getUserByEmail,
  logoutUser,
};
